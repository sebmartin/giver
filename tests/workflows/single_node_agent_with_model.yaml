name: single-node-agent-with-model
nodes:
  - name: plan
    type: agent
    model: claude-haiku-4
    steps:
      - prompt: "resume the thread"
      - prompt: "plan the next step and save it as an artifact"
        model: claude-opus-4
